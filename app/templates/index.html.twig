<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="icon" href="favicon.ico" type="image/x-icon">
    <link rel="manifest" href="/site.webmanifest">

    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">

    <script
            src="https://code.jquery.com/jquery-3.5.1.min.js"
            integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
            crossorigin="anonymous"></script>
    <script src="{{ asset('assets/js/script.js') }}"></script>
    <title>Главная</title>
</head>
<body>
{% include 'templateSite/sidebar.html.twig' %}
<main>
    {% include 'templateSite/header.html.twig' %}
    <div class="container">
        <div class="content">
            <h2 class="ttl">Popular cases</h2>
            <div class="contentProducts">
                <div class="contentProductsItem">
                    {#/////////////////#}
                </div>
            </div>

            {% for cscase, products in productsMap %}
                <h2 class="ttl">{{ cscase }}</h2>
                <div class="contentProducts">
                    {% for product in products %}
                        <div class="contentProductsItem">
                            <a href="{{ path ('case_page') }}"><img src="assets/img/{{ product.imagePath }}" alt=""
                                                                   width="225px" height="225px"></a>
                            <span class="label discount">4 дня</span>
                            <span class="label temp">1 день</span>
                            <h4>{{ product.name }}</h4>
                            <a class="btn" href="{{ path('case_id', { 'id': product.id }) }}">{{ product.price }}
                                UAH</a>
                        </div>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    </div>
    {% include 'templateSite/footer.html.twig' %}
</main>
{% include 'templateSite/modalForm.html.twig' %}
<a href="#" id="toTop">
    <img src="{{ asset('assets/img/toTop.svg') }}" alt="Go to top">
</a>

</body>
</html>